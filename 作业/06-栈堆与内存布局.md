# 作业 06：栈 / 堆 / 内存布局

> 老规矩：**先预测再跑**。本课有 sanitizer 加持：编译时加 `-fsanitize=address -g`，越界/UAF/double-free 会被当场抓住（比 valgrind 简单，先装：`sudo apt install gcc` 已有就自带）。
> 只把"卡住的 / 想确认的"发我。配套讲义：`课本/06-栈堆与内存布局.md`。

---

## 1. `[考：知识点1]` 上机：打印六大区地址

把课本知识点 1 的"眼见为实"代码跑一遍，**先预测**这 6 个地址从低到高的**排序**，再贴输出对照：

**预测**（从低到高）：

**输出**：

## 2. `[考：知识点1]` 归位题

下面每个变量各在哪个区？（text / .data / .bss / 堆 / 栈 / .rodata）

```c
int g1 = 42;             // ①
int g2;                  // ②
static int g3;           // ③
int main(void) {
    int a = 1;           // ④
    static int b = 2;    // ⑤
    int *p = malloc(40); // ⑥ p 在哪？*p（那块内存）在哪？⑦
    char *s = "hi";      // ⑧ s 在哪？"hi" 在哪？⑨
}
```

**答**（①~⑨）：

## 3. `[考：知识点2]` 上机：看栈向下长

跑课本知识点 2 的递归代码（depth 改成 5）。**预测**：每层打印的地址是越来越大还是越来越小？贴输出：

**答**：

## 4. `[考：知识点2]` 爆栈实验

```c
int main(void) {
    int a[10 * 1024 * 1024];   // 40MB 的栈上数组
    a[0] = 1;
    return 0;
}
```

**预测**：能跑吗？跑一下验证（跑完 `echo $?` 看退出码，139=段错误）。再把数组改成 `static int`（去别处了），再跑一次。两次结果+原因：

**答**：

## 5. `[考：知识点3]` 经典找错

```c
int* get_num(void) {
    int x = 42;
    return &x;
}
int main(void) {
    int *p = get_num();
    printf("%d\n", *p);
}
```

错在哪？**为什么可能打印出 42 却仍是错的**？写出三种修法：

**答**：

## 6. `[考：知识点3]` 判断改错

"函数里不能返回局部变量。"——这句话对吗？错在哪两个字上？

**答**：

## 7. `[考：知识点4]` malloc 全流程（先预测再跑）

```c
#include <stdio.h>
#include <stdlib.h>
int main(void) {
    int *p = malloc(5 * sizeof(int));
    printf("申请后未初始化，内容：\n");
    for (int i = 0; i < 5; i++) printf("%d ", p[i]);
    printf("\n");
    for (int i = 0; i < 5; i++) p[i] = (i+1) * 10;
    for (int i = 0; i < 5; i++) printf("%d ", p[i]);
    printf("\n");
    free(p);
    return 0;
}
```

**预测**：第一行打出什么（确定值还是垃圾）？第二行呢？

## 8. `[考：知识点5]` 抓泄漏实验（sanitizer 上场）

```c
#include <stdlib.h>
int main(void) {
    char *p = malloc(100);
    p[0] = 'x';        // 故意不 free
    return 0;
}
```

用 `gcc -g -fsanitize=address leak.c -o leak && ./leak` 跑。**贴出报告的最后几行**（AddressSanitizer 会告诉你漏了多少字节、哪一行 malloc 的）。加上 `free(p);` 再跑，确认报告消失。

**答**：

## 9. `[考：知识点5]` 四大错误实操（sanitizer 各抓一次）

分别写四个小程序，用 `-fsanitize=address -g` 编译运行：① 越界写（`p[10]=1`）② double free ③ use-after-free ④ free 一个栈地址。每个**贴 ASan 报错的第一行**（错误类型名），感受一下它们各长什么样：

**答**：

## 10. `[考：知识点6]` 面试一句话

不看课本，用自己的话写"栈和堆的区别"（至少 5 个维度，像面试口头回答那样组织）：

**答**：

## 11. `[考：知识点7]` 笔试经典

```c
char *a = "hello";
char  b[] = "hello";
a[0] = 'H';
b[0] = 'H';
```

哪行出问题？出什么问题？为什么 `b` 就没事？（可跑验证，注意用 sanitizer 更清楚）

**答**：

## 12. `[考：知识点7]` 综合·上机

定义 `int big[10000000];` 全局数组（不初始化）编一个程序；再写一个 `int big2[10000000] = {0};`（初始化为 0，注意写法上等价全零初始化即可）。两个都编译，`ls -l` 对比可执行文件大小。**哪个大？大多少？为什么？**

**答**：

---

> 做完自检：Q2 全对 + Q5 能讲清三种修法 + Q10 能说满 5 个维度 = 本课 pass。
> 总结欠账一路飙升：02b、03、04，加这次的 05、06……欠太多就挑 02b 和 06 补上，其余可跳 😄

# 作业 02b：指针与数组、算术、const 指针

> 新流程：**每题先预测，能跑的编译运行验证**；跑出来跟预测一致 = 会了，不用等我。const 那几题编译验证（不合法的会报错）。
> 只把"卡住的 / 想确认的"发我。配套讲义：`课本/02b-指针与数组.md`。

---

## 1. `[考：知识点1、2]`

```c
int a[3] = {10, 20, 30};
int *p = a;
printf("%d %d\n", *p, *(p+2));
```

**预测**：（然后编译运行验证）

## 2. `[考：知识点2]`

```c
int a[3] = {10, 20, 30};
printf("%d %d\n", a[1], *(a+1));
```

**预测**：这两个数一样吗？为什么？

## 3. `[考：知识点3]`

```c
int a[5] = {0, 10, 20, 30, 40};
int *p = a;
printf("%d\n", *(p + 3));
```

**预测**：

## 4. `[考：知识点3]` 步长判断

```c
int a[4] = {1, 2, 3, 4};
int *p = a;
char *c = (char*)a;
```

`p + 1` 跳 ____ 字节，`c + 1` 跳 ____ 字节。**为什么不一样？**

**答**：

## 5. `[考：知识点4]` 写代码

用**指针**（不能用 `a[i]` 下标）遍历打印 `int a[5] = {10,20,30,40,50};` 的每个元素。

**代码**：

## 6. `[考：知识点5]`

```c
int a[5];
int *p1 = &a[1], *p2 = &a[4];
printf("%ld\n", p2 - p1);
```

**预测**：结果是字节数还是元素个数？是多少？

## 7. `[考：知识点6]`

```c
int a[5];
int *p = a;
printf("%zu %zu\n", sizeof(a), sizeof(p));
```

**预测**：

## 8. `[考：知识点6]` 经典坑

```c
void print_size(int arr[]) {
    printf("%zu\n", sizeof(arr));
}
int main(void) {
    int a[5];
    print_size(a);
    return 0;
}
```

**预测**：打印多少？**为什么不是 20？**

## 9. `[考：知识点6]`

```c
int a[5];
a++;
```

能编译过吗？**为什么？**

**答**：

## 10. `[考：知识点7]` 逐行判断是否合法（编译验证：不合法的会报错）

```c
int a = 1, b = 2;
const int *p1 = &a;
int * const p2 = &a;

*p1 = 10;    // 合法？
p1 = &b;     // 合法？
*p2 = 10;    // 合法？
p2 = &b;     // 合法？
```

逐行判断（合法/不合法）+ 为什么：

## 11. `[考：综合]` 找错

```c
int a[3] = {1, 2, 3};
int *p = a;
printf("%d\n", *(p + 6));
```

**问题**：

## 12. `[考：综合·理解]` 不用跑

为什么 C 函数接收数组时，通常还要**再传一个长度参数** `void foo(int a[], int n)`？

**答**：

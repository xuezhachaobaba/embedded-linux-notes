# 作业 02：指针（基础）

> 在每题下面写答案。**写完保存，发"好了"**，我来批改。
> 配套讲义：`课本/02-指针.md`。**每道题都用 gcc 实际编译运行验证**（你有 gcc），别只用脑子猜。

---

## 1. 下面代码输出什么？

```c
int a = 10;
int *p = &a;
*p = 20;
printf("%d\n", a);
```

**输出**：

**为什么**：

## 2. 下面代码输出什么？

```c
int a = 5;
int *p = &a;
printf("%d %d\n", a, *p);
```

**输出**：

## 3. 这段代码有什么问题？怎么改？

```c
int *p;
*p = 10;
```

**问题**：

**正确写法**：

## 4. 在 64 位系统上

```c
printf("%zu %zu\n", sizeof(int*), sizeof(char*));
```

**输出**：______

这两个数**一样吗？为什么**？

## 5.（理解）为什么下面代码里，main 中 `x` 的值不会变成 6？

```c
void add_one(int n) {
    n = n + 1;
}

int main(void) {
    int x = 5;
    add_one(x);
    // x 还是 5，不是 6
    return 0;
}
```

**答**：
